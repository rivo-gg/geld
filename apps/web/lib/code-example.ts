export const codeExample =
  'import { iban } from "@rivo-gg/geld"\r\n\r\nexport function Component() {\r\n  \r\n  const validateIban = (input: string) => {\r\n    if (!input) return console.error("Enter an IBAN")\r\n\r\n    iban.isValid(input)\r\n      ? console.info("Valid IBAN")\r\n      : console.error("Invalid IBAN")\r\n  }\r\n\r\n  return (\r\n    <Input\r\n      onBlur={(e) => validateIban(e.target.value)}\r\n      placeholder="Insert an IBAN"\r\n    />\r\n  )\r\n}\r'
